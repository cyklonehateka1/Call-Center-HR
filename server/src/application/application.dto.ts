export interface QuestionDto {
  language: string;
}

export interface ApplicationDataDto {
  language: string;
  questions: string[];
  name: string;
  email: string;
  phone: string;
  recording;
}
